spring:
  application:
    name: vehicle-service
  kafka:
    bootstrap-servers: "http://localhost:29092"
    consumer:
      group-id: fleet.tripstarted.to.vehicle
      auto-offset-reset: earliest
  datasource:
    url: jdbc:postgresql://localhost:5555/fleetVehicles
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
fleet-management:
  config:
    kafka:
      server-url: "http://localhost:29092"
      topics:
        trip-started: "fleet.trip.started"
        trip-ended: "fleet.trip.ended"
server:
  port: 8082
